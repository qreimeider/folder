const { checkPath, checkPathAsync } = require("./check");
const { createPath, createPathAsync } = require("./create");

module.exports = {
	checkPath,
	createPath,

	checkPathAsync,
	createPathAsync,
};
